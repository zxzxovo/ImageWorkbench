# Third-party notices

The application uses third-party packages listed in `package.json`,
`bun.lock`, and `src-tauri/Cargo.toml`/`Cargo.lock`. Those packages retain
their own copyright and license terms. Release jobs publish the exact source
manifests and generated checksums alongside artifacts so downstream users can
audit the dependency set. This file is intentionally a pointer rather than a
reproduction of every dependency license.
