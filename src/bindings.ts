// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core";

/** Commands */
export const commands = {
	workspaceLoad: () => typedError<{
	locale: Locale,
	activeProjectId: string,
	projects: ProjectDto[],
	providers: ProviderProfileDto_Serialize[],
	history: HistoryRecordDto[],
} | null, CommandError>(__TAURI_INVOKE("workspace_load")),
	workspaceSave: (snapshot: WorkspaceSnapshot_Deserialize) => typedError<null, CommandError>(__TAURI_INVOKE("workspace_save", { snapshot })),
	queuePause: () => __TAURI_INVOKE<boolean>("queue_pause"),
	queueResume: () => __TAURI_INVOKE<boolean>("queue_resume"),
	queueStatus: () => __TAURI_INVOKE<boolean>("queue_status"),
	providerSecretGet: (providerId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("provider_secret_get", { providerId })),
	providerSecretExists: (providerId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("provider_secret_exists", { providerId })),
	providerSecretSet: (providerId: string, apiKey: string) => typedError<null, CommandError>(__TAURI_INVOKE("provider_secret_set", { providerId, apiKey })),
	providerSecretDelete: (providerId: string) => typedError<null, CommandError>(__TAURI_INVOKE("provider_secret_delete", { providerId })),
	providerHeaderSecretSet: (providerId: string, headerId: string, value: string) => typedError<null, CommandError>(__TAURI_INVOKE("provider_header_secret_set", { providerId, headerId, value })),
	providerHeaderSecretExists: (providerId: string, headerId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("provider_header_secret_exists", { providerId, headerId })),
	providerHeaderSecretDelete: (providerId: string, headerId: string) => typedError<null, CommandError>(__TAURI_INVOKE("provider_header_secret_delete", { providerId, headerId })),
	providerDelete: (providerId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("provider_delete", { providerId })),
	providerTest: (provider: ProviderProfileDto_Deserialize) => typedError<boolean, CommandError>(__TAURI_INVOKE("provider_test", { provider })),
	providerSyncModels: (provider: ProviderProfileDto_Deserialize) => typedError<string[], CommandError>(__TAURI_INVOKE("provider_sync_models", { provider })),
	generateImages: (request: GenerateImagesRequest_Deserialize) => typedError<GenerationCommandResult, CommandError>(__TAURI_INVOKE("generate_images", { request })),
	generateImagesLegacy: (request: GenerateImagesRequest_Deserialize) => typedError<GeneratedAsset[], CommandError>(__TAURI_INVOKE("generate_images_legacy", { request })),
	revealPath: (path: string) => typedError<null, CommandError>(__TAURI_INVOKE("reveal_path", { path })),
	exportAsset: (sourcePath: string, destinationPath: string) => typedError<null, CommandError>(__TAURI_INVOKE("export_asset", { sourcePath, destinationPath })),
	projectCreate: (projectId: string, name: string, path: string) => typedError<ProjectSummary, CommandError>(__TAURI_INVOKE("project_create", { projectId, name, path })),
	projectOpen: (path: string) => typedError<ProjectSummary, CommandError>(__TAURI_INVOKE("project_open", { path })),
	projectLoadDetails: (projectId: string, recentRunLimit: number | null) => typedError<ProjectDetailsDto, CommandError>(__TAURI_INVOKE("project_load_details", { projectId, recentRunLimit })),
	projectClose: (projectId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("project_close", { projectId })),
	projectsOpenList: () => typedError<ProjectSummary[], CommandError>(__TAURI_INVOKE("projects_open_list")),
	projectImportInputs: (projectId: string, paths: string[]) => typedError<ImportedInputDto[], CommandError>(__TAURI_INVOKE("project_import_inputs", { projectId, paths })),
	referencePreview: (projectId: string, path: string) => typedError<string | null, CommandError>(__TAURI_INVOKE("reference_preview", { projectId, path })),
	projectAssetDataUrl: (projectId: string, path: string) => typedError<string | null, CommandError>(__TAURI_INVOKE("project_asset_data_url", { projectId, path })),
	promptContextList: (projectId: string) => typedError<PromptContext[], CommandError>(__TAURI_INVOKE("prompt_context_list", { projectId })),
	promptContextUpsert: (projectId: string, context: PromptContext) => typedError<null, CommandError>(__TAURI_INVOKE("prompt_context_upsert", { projectId, context })),
	promptContextDelete: (projectId: string, contextId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("prompt_context_delete", { projectId, contextId })),
	generationPresetList: (projectId: string) => typedError<GenerationPreset[], CommandError>(__TAURI_INVOKE("generation_preset_list", { projectId })),
	generationPresetUpsert: (projectId: string, preset: GenerationPreset) => typedError<null, CommandError>(__TAURI_INVOKE("generation_preset_upsert", { projectId, preset })),
	generationPresetDelete: (projectId: string, presetId: string) => typedError<boolean, CommandError>(__TAURI_INVOKE("generation_preset_delete", { projectId, presetId })),
	historyList: (projectId: string, limit: number, offset: number) => typedError<RunRecord[], CommandError>(__TAURI_INVOKE("history_list", { projectId, limit, offset })),
	historyGet: (projectId: string, runId: string) => typedError<{
	id: string,
	groupId: string | null,
	request: GenerationRequest,
	status: RunStatus,
	rawPrompt: string,
	finalPrompt: string,
	contextSnapshot?: PromptContext[],
	presetSnapshot: GenerationPreset | null,
	capabilitySnapshot: ModelCapability,
	capabilityRegistryVersion: string,
	modelVersion: string | null,
	providerRequestId: string | null,
	redactedRequest: unknown | null,
	redactedResponse: unknown | null,
	startedAt: string | null,
	finishedAt: string | null,
	createdAt: string,
	updatedAt: string,
} | null, CommandError>(__TAURI_INVOKE("history_get", { projectId, runId })),
	historyDetails: (projectId: string, runId: string) => typedError<{
	run: RunRecord,
	outputs: HistoryOutputDto[],
	usage: UsageRecord[],
	errors: ErrorRecord[],
} | null, CommandError>(__TAURI_INVOKE("history_details", { projectId, runId })),
	historyDelete: (projectId: string, runId: string, deleteLocalAssets: boolean, deleteRemoteFiles: boolean) => typedError<HistoryMutationResultDto, CommandError>(__TAURI_INVOKE("history_delete", { projectId, runId, deleteLocalAssets, deleteRemoteFiles })),
	historyClear: (projectId: string, deleteLocalAssets: boolean, deleteRemoteFiles: boolean) => typedError<HistoryMutationResultDto, CommandError>(__TAURI_INVOKE("history_clear", { projectId, deleteLocalAssets, deleteRemoteFiles })),
	runCancel: (runId: string, projectId: string | null) => typedError<boolean, CommandError>(__TAURI_INVOKE("run_cancel", { runId, projectId })),
	remoteTasksPoll: (projectId: string) => typedError<GenerationCommandResult[], CommandError>(__TAURI_INVOKE("remote_tasks_poll", { projectId })),
	projectRemapProvider: (projectId: string, fromProviderId: string, toProviderId: string) => typedError<ProviderRemapResult, CommandError>(__TAURI_INVOKE("project_remap_provider", { projectId, fromProviderId, toProviderId })),
};

/* Types */
export type ApiMode = "native" | "openai-compatible";

export type Background = "auto" | "opaque" | "transparent";

export type CommandError = {
	code: string,
	message: string,
	details: unknown | null,
};

export type CommonDescriptionDto = {
	id: string,
	title: string,
	content: string,
	enabled: boolean,
	placement: DescriptionPlacement,
	createdAt: string,
};

export type ContextPlacement = "prepend" | "append";

export type DescriptionPlacement = "prefix" | "suffix";

export type ErrorRecord = {
	id: string,
	runId: string,
	jobId: string | null,
	error: ProviderError,
	createdAt: string,
};

export type ExecutionMode = "realtime" | "background" | "provider_batch";

export type FrontendAuthScheme = "bearer" | "header" | "query";

export type FrontendGenerationPreset = {
	id: string,
	name: string,
	description: string,
	providerId: string,
	model: string,
	mode: GenerationMode,
	aspectRatio: string,
	size: string,
	quality: string,
	outputFormat: string,
	responseFormat: string | null,
	promptTemplate: string,
	createdAt: string,
};

export type FrontendProviderKind = "openai" | "xai" | "gemini" | "custom";

export type FrontendTaskStatus = "queued" | "running" | "completed" | "failed";

export type GenerateImagesRequest = GenerateImagesRequest_Serialize | GenerateImagesRequest_Deserialize;

export type GenerateImagesRequest_Deserialize = {
	clientTaskId: string | null,
	projectId: string,
	storagePath: string,
	provider: ProviderProfileDto_Deserialize,
	draft: GenerationDraftDto,
	composedPrompt: string,
	contextIds?: string[],
	presetId: string | null,
	contextSnapshot?: PromptContext[],
	presetSnapshot: GenerationPreset | null,
};

export type GenerateImagesRequest_Serialize = {
	clientTaskId: string | null,
	projectId: string,
	storagePath: string,
	provider: ProviderProfileDto_Serialize,
	draft: GenerationDraftDto,
	composedPrompt: string,
	contextIds: string[],
	presetId: string | null,
	contextSnapshot: PromptContext[],
	presetSnapshot: GenerationPreset | null,
};

export type GeneratedAsset = {
	id: string,
	taskId: string,
	url: string,
	filePath: string,
	width: number,
	height: number,
	format: string,
	prompt: string,
	createdAt: string,
	selected: boolean | null,
};

export type GenerationCommandResult = {
	runId: string,
	requestId: string,
	interactionId: string | null,
	failureReason: string | null,
	assets?: GeneratedAsset[],
	responseParts?: ResponsePartDto[],
	usage?: UsageDto,
};

export type GenerationDraftDto = {
	providerId: string,
	model: string,
	mode: GenerationMode,
	prompt: string,
	negativePrompt: string,
	aspectRatio: string,
	size: string,
	quality: string,
	count: number,
	outputFormat: string,
	responseFormat: string | null,
	background: string,
	compression: number,
	references?: ReferenceAssetDto[],
	maskDataUrl: string | null,
	seed: string,
	inputFidelity: string,
	thinkingLevel: string,
	webSearch: boolean,
	imageSearch: boolean,
	includeText: boolean,
	stream: boolean,
	backgroundTask: boolean,
	batch: boolean,
	partialImages: number,
	serviceTier: string,
	temperature: number | null,
	topP: number | null,
	storeInteraction: boolean,
	customJson: string,
	previousResponseId: string | null,
	previousInteractionId: string | null,
	useResponsesApi: boolean | null,
	useInteractionsApi: boolean | null,
	responseModel: string | null,
	imageGenerationAction: string | null,
	moderation: string | null,
	style: string | null,
	user: string | null,
	xaiStorageFilename: string | null,
	xaiExpiresAfter: number | null,
	xaiPublicUrl: boolean | null,
	xaiPublicUrlExpiresAfter: number | null,
	resumeInteractionId: string | null,
	lastEventId: string | null,
	outputFilename: string,
	flatOutput: boolean,
};

export type GenerationEventEnvelope = {
	runId: string,
	event: unknown,
};

export type GenerationMode = "generate" | "edit" | "mask" | "video" | "variation" | "conversation_continue";

export type GenerationPreset = {
	id: string,
	name: string,
	providerProfileId: string | null,
	modelId: string | null,
	operation: Operation | null,
	parameters?: { [key in string]: unknown },
	output: OutputSpec | null,
	createdAt: string,
	updatedAt: string,
};

export type GenerationRequest = {
	id: string,
	projectId: string,
	runGroupId: string | null,
	providerProfileId: string,
	modelId: string,
	operation: Operation,
	executionMode: ExecutionMode,
	prompt: string,
	finalPrompt: string | null,
	contextIds?: string[],
	presetId: string | null,
	conversationId: string | null,
	previousInteractionId: string | null,
	inputs?: InputAsset[],
	mask: InputAsset | null,
	output?: OutputSpec,
	parameters?: { [key in string]: unknown },
	metadata?: { [key in string]: unknown },
	createdAt: string,
};

export type GenerationTaskDto = {
	id: string,
	projectId: string,
	providerId: string,
	providerName: string,
	model: string,
	prompt: string,
	composedPrompt: string,
	mode: GenerationMode,
	status: FrontendTaskStatus,
	progress: number | null,
	count: number,
	createdAt: string,
	durationMs: number | null,
	error: string | null,
	assets?: GeneratedAsset[],
	responseParts?: ResponsePartDto[],
	requestId: string | null,
	interactionId: string | null,
	usage: UsageDto | null,
};

export type HistoryDetailsDto = {
	run: RunRecord,
	outputs: HistoryOutputDto[],
	usage: UsageRecord[],
	errors: ErrorRecord[],
};

export type HistoryMutationResultDto = {
	requestedRuns: number,
	deletedRuns: number,
	localAssetsDeleted: number,
	remoteFilesDeleted: number,
	remoteFilesRetained: number,
	failures?: LifecycleFailureDto[],
};

export type HistoryOutputDto = {
	output: OutputPart,
	previewDataUrl: string | null,
	width: number | null,
	height: number | null,
};

export type HistoryRecordDto = {
	favorite: boolean,
} & GenerationTaskDto;

export type ImageFormat = "png" | "jpeg" | "webp";

export type ImageSize = { mode: "auto" } | { mode: "preset"; value: string } | { mode: "custom"; width: number; height: number };

export type ImportedInputDto = {
	relativePath: string,
	mimeType: string,
	sha256: string,
	sizeBytes: number,
	width: number | null,
	height: number | null,
};

export type InputAsset = {
	id: string,
	kind: InputAssetKind,
	source: InputSource,
	mimeType: string | null,
	role: string | null,
	label: string | null,
	sha256: string | null,
	sizeBytes: number | null,
	metadata?: { [key in string]: unknown },
};

export type InputAssetKind = "image" | "mask" | "video";

export type InputSource = { type: "local_path"; path: string } | { type: "url"; url: string } | { type: "base64"; data: string } | { type: "provider_file"; file_id: string } | { type: "generated_output"; output_part_id: string };

export type LifecycleFailureDto = {
	runId: string | null,
	providerProfileId: string | null,
	remoteFileId: string | null,
	code: string,
	message: string,
};

export type Locale = "zh-CN" | "en-US";

export type ModelCapability = {
	schemaVersion: number,
	providerKind: ProviderKind,
	modelId: string,
	displayName: string,
	aliases?: string[],
	documentationUrl: string | null,
	operations?: Operation[],
	executionModes?: ExecutionMode[],
	inputKinds?: InputAssetKind[],
	outputModalities?: OutputModality[],
	maxPromptChars: number | null,
	maxReferenceImages: number,
	maxVideoInputs: number,
	maxOutputs: number,
	maxInlineBytes: number | null,
	maxFileBytes: number | null,
	sizes?: SizeCapability,
	aspectRatios?: string[],
	resolutions?: string[],
	qualities?: string[],
	formats?: ImageFormat[],
	responseFormats?: ResponseFormat[],
	backgrounds?: Background[],
	styles?: string[],
	moderationModes?: string[],
	supportsMask: boolean,
	supportsStreaming: boolean,
	supportsPartialImages: boolean,
	supportsInterleavedOutput: boolean,
	supportsThinking: boolean,
	supportsSearch: boolean,
	supportsProviderFiles: boolean,
	supportsRemoteStorage: boolean,
	supportsTtl: boolean,
	minTtlSeconds: number | null,
	maxTtlSeconds: number | null,
	allowUnknownParameters: boolean,
	parameterRules?: { [key in string]: ParameterRule },
	defaults?: { [key in string]: unknown },
};

export type Operation = "generate" | "edit" | "variation" | "conversation_continue" | "video_reference_to_image";

export type OutputModality = "image" | "text" | "thought" | "citation";

export type OutputPart = {
	id: string,
	runId: string,
	jobId: string | null,
	sequence: number,
	kind: OutputPartKind,
	text: string | null,
	localPath: string | null,
	remoteUrl: string | null,
	providerFileId: string | null,
	mimeType: string | null,
	sha256: string | null,
	sizeBytes: number | null,
	metadata?: { [key in string]: unknown },
	createdAt: string,
};

export type OutputPartKind = "image" | "text" | "thought" | "citation" | "search_suggestion";

export type OutputSpec = {
	count: number,
	size?: ImageSize,
	aspectRatio: string | null,
	resolution: string | null,
	quality: string | null,
	format: ImageFormat | null,
	compression: number | null,
	background: Background | null,
	style: string | null,
	moderation: string | null,
	responseFormat: ResponseFormat | null,
	modalities?: OutputModality[],
	remoteStorage: boolean | null,
	ttlSeconds: number | null,
};

export type ParameterRule = {
	valueKind: ParameterValueKind,
	required: boolean,
	allowedValues?: unknown[],
	min: number | null,
	max: number | null,
};

export type ParameterValueKind = "any" | "boolean" | "integer" | "number" | "string" | "array" | "object";

export type ProjectDetailsDto = {
	summary: ProjectSummary,
	contexts: PromptContext[],
	presets: GenerationPreset[],
	recentRecords: HistoryDetailsDto[],
};

export type ProjectDto = {
	id: string,
	name: string,
	description: string,
	storagePath: string,
	createdAt: string,
	updatedAt: string,
	color: string,
	descriptions?: CommonDescriptionDto[],
	presets?: FrontendGenerationPreset[],
	settings: ProjectSettingsDto,
};

export type ProjectSettingsDto = {
	useCommonDescriptions: boolean,
	saveMetadata: boolean,
	saveRawResponse: boolean,
	autoOpenFolder: boolean,
	namingPattern: string,
	defaultProviderId: string,
	defaultModel: string,
	flatOutput: boolean,
	defaultStream: boolean | null,
};

export type ProjectSummary = {
	id: string,
	name: string,
	rootPath: string,
	createdAt: string,
	updatedAt: string,
	lastOpenedAt: string,
	defaultProviderProfileId: string | null,
	defaultModelId: string | null,
	defaultParameters?: { [key in string]: unknown },
};

export type PromptContext = {
	id: string,
	name: string,
	content: string,
	placement: ContextPlacement,
	sortOrder: number,
	enabled: boolean,
	createdAt: string,
	updatedAt: string,
};

export type ProviderError = {
	code: string,
	message: string,
	httpStatus: number | null,
	retryable: boolean,
	requestId: string | null,
	provider: ProviderKind | null,
	details: unknown | null,
};

export type ProviderHeaderDto = {
	id: string,
	name: string,
	value?: string,
	secret: boolean,
	hasStoredValue: boolean | null,
};

export type ProviderKind = "open_ai" | "xai" | "gemini" | "open_ai_compatible";

export type ProviderProfileDto = ProviderProfileDto_Serialize | ProviderProfileDto_Deserialize;

export type ProviderProfileDto_Deserialize = {
	id: string,
	name: string,
	kind: FrontendProviderKind,
	baseUrl: string,
	apiKey?: string,
	hasStoredSecret: boolean | null,
	apiMode: ApiMode,
	enabled: boolean,
	models?: string[],
	apiVersion: string | null,
	organization: string | null,
	projectId: string | null,
	customHeader: string | null,
	lastSyncedAt: string | null,
	timeoutSeconds: number | null,
	timeoutMs: number | null,
	proxyUrl: string | null,
	authScheme: FrontendAuthScheme | null,
	authHeaderName: string | null,
	authPrefix: string | null,
	authQueryName: string | null,
	customHeaders?: ProviderHeaderDto[],
	modelsPath: string | null,
	compatibilityJson: string | null,
	capabilityOverridesJson: string | null,
	defaultStream: boolean | null,
} & {
	compatibilityJson: string | null,
} | {
	extraJson: string | null,
};

export type ProviderProfileDto_Serialize = {
	id: string,
	name: string,
	kind: FrontendProviderKind,
	baseUrl: string,
	apiKey: string,
	hasStoredSecret: boolean | null,
	apiMode: ApiMode,
	enabled: boolean,
	models: string[],
	apiVersion: string | null,
	organization: string | null,
	projectId: string | null,
	customHeader: string | null,
	lastSyncedAt: string | null,
	timeoutSeconds: number | null,
	timeoutMs: number | null,
	proxyUrl: string | null,
	authScheme: FrontendAuthScheme | null,
	authHeaderName: string | null,
	authPrefix: string | null,
	authQueryName: string | null,
	customHeaders: ProviderHeaderDto[],
	modelsPath: string | null,
	compatibilityJson: string | null,
	capabilityOverridesJson: string | null,
	defaultStream: boolean | null,
};

export type ProviderRemapResult = {
	summaries: number,
	presets: number,
	conversations: number,
	runs: number,
	jobs: number,
	remoteTasks: number,
	remoteFiles: number,
};

export type ReferenceAssetDto = {
	id: string,
	name: string,
	url: string,
	mimeType: string,
	role: ReferenceRole,
	sourceType: ReferenceSourceType | null,
	fileId: string | null,
};

export type ReferenceRole = "object" | "character" | "style" | "source" | "video";

export type ReferenceSourceType = "local" | "url" | "base64" | "file-id";

export type ResponseFormat = "url" | "base64_json" | "inline" | "provider_file";

export type ResponsePartDto = { type: "image"; id: string; asset_id: string; url: string; mime_type: string; width: number; height: number; file_path: string } | { type: "text"; id: string; text: string } | { type: "thought"; id: string; summary: string; image_url: string | null } | { type: "citation"; id: string; title: string | null; url: string | null; snippet: string | null; start_index: number | null; end_index: number | null } | { type: "search_suggestions"; id: string; html: string } | { type: "remote_file"; id: string; name: string; uri: string; mime_type: string; size_bytes: number | null } | { type: "remote_job"; id: string; job_id: string; kind: string; status: string; provider: string; model: string | null } | { type: "usage"; id: string; usage: UsageDto } | { type: "request_meta"; id: string; request_id: string; interaction_id: string | null; provider_response_id: string | null };

export type RunRecord = {
	id: string,
	groupId: string | null,
	request: GenerationRequest,
	status: RunStatus,
	rawPrompt: string,
	finalPrompt: string,
	contextSnapshot?: PromptContext[],
	presetSnapshot: GenerationPreset | null,
	capabilitySnapshot: ModelCapability,
	capabilityRegistryVersion: string,
	modelVersion: string | null,
	providerRequestId: string | null,
	redactedRequest: unknown | null,
	redactedResponse: unknown | null,
	startedAt: string | null,
	finishedAt: string | null,
	createdAt: string,
	updatedAt: string,
};

export type RunStatus = "queued" | "running" | "paused" | "succeeded" | "partially_succeeded" | "failed" | "cancelled" | "interrupted";

export type SizeCapability = {
	supportsAuto: boolean,
	presets?: string[],
	allowCustom: boolean,
	minWidth: number | null,
	maxWidth: number | null,
	minHeight: number | null,
	maxHeight: number | null,
	maxArea: number | null,
	widthMultipleOf: number | null,
	heightMultipleOf: number | null,
	minAspectRatio: number | null,
	maxAspectRatio: number | null,
};

export type UsageDto = {
	inputTokens: number,
	outputTokens: number,
	thoughtTokens: number,
	cachedTokens: number,
	totalTokens: number,
	generatedImages: number,
	imageTokens: number | null,
	costUsd: number | null,
};

export type UsageRecord = {
	id: string,
	runId: string,
	jobId: string | null,
	inputTokens: number | null,
	outputTokens: number | null,
	imageCount: number | null,
	inputBytes: number | null,
	outputBytes: number | null,
	costMicros: number | null,
	currency: string | null,
	details?: { [key in string]: unknown },
	createdAt: string,
};

export type WorkspaceSnapshot = WorkspaceSnapshot_Serialize | WorkspaceSnapshot_Deserialize;

export type WorkspaceSnapshot_Deserialize = {
	locale: Locale,
	activeProjectId: string,
	projects?: ProjectDto[],
	providers?: ProviderProfileDto_Deserialize[],
	history?: HistoryRecordDto[],
};

export type WorkspaceSnapshot_Serialize = {
	locale: Locale,
	activeProjectId: string,
	projects: ProjectDto[],
	providers: ProviderProfileDto_Serialize[],
	history: HistoryRecordDto[],
};

/* Tauri Specta runtime */
async function typedError<T, E>(result: Promise<T>): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
    try {
        return { status: "ok", data: await result };
    } catch (e) {
        if (e instanceof Error) throw e;
        return { status: "error", error: e as any };
    }
}

